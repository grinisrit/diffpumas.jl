# Julia
*.jl.cov
*.jl.*.cov
*.jl.mem
.DS_Store
*.orig
Manifest.toml
*.html
*.log

# IDE & notebooks files
.vscode
.idea
.ipynb_checkpoints/
.mypy_cache/
.DS_Store
.vs/
venv
.venv/
# Cursor — commit domain skills; ignore personal overlay
.cursor/*
!.cursor/skills/
!.cursor/skills/**
.cursor/skills/activate-repo/
.cursorrules
*.code-workspace
.julia*
*.zip

# examples/data — generated outputs; see lvd_conf exceptions for committed CSV/JSON
examples/data/*.html
examples/data/*.csv
!examples/data/flux_comparison.csv
examples/data/*.bin

# PUMAS physics binary dumps — never commit (any path, including lvd_conf)
*.pumas

# LVD generated outputs; keep the committed lvd_conf input data visible.
examples/data/lvd_*/
examples/data/*_backup_*/
# Re-include lvd_conf LAST so the input data stays tracked (must follow the globs above).
!examples/data/lvd_conf/
!examples/data/lvd_conf/**

# LVD paper/talk bundle (local archive; diffmuontomoLVD.zip covered by *.zip)
diffmuontomoLVD/

# Claude Code — commit domain skills; ignore personal overlay
.claude/*
!.claude/skills/
!.claude/skills/**
.claude/skills/activate-repo/

# Agent Skills — commit .agents/skills/; ignore personal overlay
.agents/skills/activate-repo/

